{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the tray-only HyperDeck Adapter. Plugins are driven from Rust, so only their default permissions are granted.",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "updater:default",
    "process:default",
    "dialog:default"
  ]
}
